// for the future block will be expr
// so `let a = { a + 20 };` is possible but not for now.
// let a = 10;
// {
//     let b = 5;
//     a = a + b;
// }


let min = (a,b) :: { return; };
let add = (a,b) :: { return a + b; };
let y = (5 + 3) * 2;
let x = add(10, 12);
